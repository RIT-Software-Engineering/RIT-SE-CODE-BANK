# events {}

# http {
#     server {
#         listen 80;

#     # API requests forwarded to backend under the same /cmt prefix
#     # must be listed first so it catches /cmt/api before the general /cmt/ rule
#     location /api/ {
#         proxy_pass http://cmt-backend:5010/;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }

#     # serve the frontend under /cmt
#     location /cmt/ {
#         proxy_pass http://cmt-frontend:3010/;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }
#     }
# }

