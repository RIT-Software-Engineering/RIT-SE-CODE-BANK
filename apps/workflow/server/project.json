{
    "targets": {
        "deploy": {
            "dependsOn": ["build"],
            "executor": "nx:run-commands",
            "options": {
                "command": "./deploy/deploy-workflows.sh"
            }
        },
        "deploy-test": {
            "executor": "nx:run-commands",
            "options": {
                "command": "echo would deploy workflows here"
            }
        }
    }
}