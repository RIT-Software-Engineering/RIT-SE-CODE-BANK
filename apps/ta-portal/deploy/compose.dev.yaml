services:
  frontend:
    build:
      dockerfile: apps/ta-portal/ui/Dockerfile.dev
      args:
        NEXT_PUBLIC_NODE_ENV: "DEV"
        NEXT_PUBLIC_BACKEND_URL: "https://localhost:3300"

  backend:
    build:
      args:
        FRONTEND_URL: "https://localhost:3000"
    environment:
      NODE_ENV: "DEV"
    