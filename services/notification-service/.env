# When using docker, the docker port for stmp is 25 as opposed to 2525 (see the docker-compose.yml)
SMTP_PORT=25
SMTP_FROM=se_svc_apps@rit.edu

SMTP_HOST_UI_PORT=3005
SMTP_HOST_SMTP_PORT=2525

# Service
NOTIF_HOST_PORT=4001
NOTIF_CONTAINER_PORT=4000

DB_NAME="notification_service"
DB_APP_USER="notification_service_user"
DB_HOST_PORT=3310