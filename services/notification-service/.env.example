# SMTP (dev with smtp4dev)
SMTP_HOST=smtp4dev
SMTP_PORT=25
SMTP_FROM=se_svc_apps@rit.edu

# Service
PORT=4000
NODE_ENV=development

# Slack (bot app)
# Your bot token (xoxb-...)
SLACK_BOT_TOKEN=

# MariaDB connection (example for local dev using docker run)
# We use port 3310 for local testing in this workspace to avoid conflicts.
DATABASE_URL="mysql://root:newPassword@localhost:3309/notification_service"